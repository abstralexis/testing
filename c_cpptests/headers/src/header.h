#ifndef HEADER      // This is a header guard. It prevents including it twice.
#define HEADER      

// Here we can define functions/prototypes that are shared

float f(float x) {
    return x*2;
}

#endif